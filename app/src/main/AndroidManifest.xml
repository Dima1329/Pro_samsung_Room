<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pro_samsung">

    <application
        android:allowBackup="true"
        android:icon="@drawable/galochka"
        android:label="@string/app_name"


        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".CheaterCabinetActivity" />
        <activity android:name=".DeleteQuestionActivity"/>
        <activity android:name=".MainActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddQuestionActivity" />
        <activity android:name=".Logo"/>

    </application>

</manifest>